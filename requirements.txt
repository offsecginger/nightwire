# sidechannel dependencies

# Core
aiohttp>=3.9.0
pyyaml>=6.0
python-dotenv>=1.0.0
structlog>=24.0.0
pydantic>=2.0.0

# Memory system (embeddings)
sentence-transformers>=2.2.0

# Optional: Vector search in SQLite (faster semantic search)
# sqlite-vec>=0.1.1

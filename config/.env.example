# nightwire Environment Variables
# Copy this file to .env and add your secrets
#
# WARNING: Never commit the .env file with real keys!

# Optional: Anthropic API key (Claude CLI handles its own auth via `claude login`)
# Only needed if running Claude outside the CLI
# ANTHROPIC_API_KEY=

# Optional: nightwire AI assistant — set one (or both) of these
# OpenAI: https://platform.openai.com/api-keys
# OPENAI_API_KEY=

# Grok: https://x.ai
# GROK_API_KEY=

# Generic: for any OpenAI-compatible provider (set api_key_env in settings.yaml)
# NIGHTWIRE_API_KEY=
